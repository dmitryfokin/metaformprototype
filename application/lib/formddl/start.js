async () => {
  //if (application.worker.id === 'W1') {
    console.debug(`Start form DDL plugin on worker ${application.worker.id}`);
    
    //lib.example.cache.set({ key: 'keyName', val: this.privateField });
    //const res =S lib.example.cache.get({ key: 'keyName' });
    //console.debug({ res, cache: lib.example.cache.values });
  //}
  //console.debug({menu: domain.menu.mainMenu});
};
